# Subform.ai 网站能力分析文档

## 1. 网站概述

### 1.1 产品定位
Subform.ai 是一个专注于“可视化编排 AI 对话”的浏览器端工具。用户在无限画布上以卡片（note）的形式撰写提示、组织分支、拖拽连线，快速比较不同输出或延伸实验路径，用于 Prompt 工程、对话拆解与灵感记录。

### 1.2 核心价值主张
- **分支对话**：将单线对话拆解为树状结构，任意分支、回溯与复用提示。
- **上下文复用**：生成新输出时自动遍历上游节点，拼接文本与图片上下文，保持连贯性。
- **可视化组织**：通过拖拽、缩放、连线等交互，在大画布上直观管理多条思路。
- **本地隐私**：所有节点、图片、API Key 均保存在浏览器端的 IndexedDB/LocalStorage，不上传云端。

## 2. 关键功能拆解

### 2.1 画布与布局
- **无限画布**：`#grid-container` 设定为 50000×50000 像素，可在 `translate + scale` 的基础上实现平移、缩放和重置视图。
- **网格背景**：利用 CSS 径向渐变绘制点阵网格，缩放时动态调整 `background-size` 与 `box-shadow`，保持视觉一致。
- **滚动控制**：自定义水平/垂直滚动条与惯性淡入淡出逻辑；同时支持空格键拖拽画布与触控板平滑缩放。

### 2.2 节点类型与结构
- **输入节点（默认 Note）**：包含标题栏、正文 `textarea`、“Generate” 按钮以及左右连接点。初始状态会自动生成 Welcome note 与 “Handy shortcuts” 说明卡。
- **输出节点（Output Note）**：只读文本卡片，标题区域显示当前生成状态/摘要，底部提供 “Copy” 按钮。
- **图片节点（Image Note）**：拖拽或粘贴图片后创建，自动根据图片宽高比调整尺寸，以 DataURL 形式存储。
- **API 菜单**：右上角浮动按钮展开为设置面板，可录入 `API Base URL`、`API Key`、`Model` 并决定是否在本机记住配置。API Key 默认显示为 “•”，保存后普通节点底部的模型标签即刻更新。

### 2.3 生成与上下文流程
- **节点创建方式**：
  - 双击画布空白：新建输入节点。
  - 双击节点左右连接点：创建已连线的新节点。
  - Option/Alt + 拖动：复制节点及其连线。
- **连线规则**：左右各有连接点，拖动形成贝塞尔曲线。连线支持高亮、虚线动画与脉冲发光效果。
- **上下文收集**：`buildContext()` 自当前节点向上游遍历，融合文本及图片说明，最终拼成 `messages` 中的 `user` 提示。
- **输出生成**：`triggerGeneration()` 先在右侧创建输出节点并连线，随后调用 `callModel()` 以 OpenAI/DeepSeek 风格向 `chat/completions` 接口发送一次性请求，解析 JSON 后写回输出节点。
- **标题策略**：取最后一条输入的首行（最多 40 字符）作为输出标题，若解析失败则退回 “Output”。

### 2.4 键鼠与触控交互
- **画布级**：
  - 空格 + 拖拽：抓取画布。
  - 鼠标滚轮 + Ctrl / 触控缩放：范围 0.125–4 倍。
  - ⌘/Ctrl + 0/+/−：重置或分级缩放。
  - 自定义滚动条：靠近窗口边缘自动显隐。
- **节点级**：
  - Shift + 点击或框选：多选节点/连线。
  - 方向键移动（Shift 加速）。
  - 八方向拖拽柄调整尺寸，移动/缩放均遵循 10px 网格对齐。
  - Enter 触发生成；Backspace/Delete 删除选中节点或连线；Esc 结束连线。
- **连线管理**：支持 Shift 选中连线、Delete 删除、非法连接抖动提醒；连接点根据是否存在连线动态高亮。
- **剪贴板支持**：粘贴图片直接生成图片节点；复制文本同时写入系统剪贴板与内部缓存，便于跨页面粘贴。

### 2.5 Onboarding 与反馈
- **欢迎卡片**：默认 note 提供产品概述、操作提示与隐私声明。
- **快捷键卡片**：输出节点列出常用快捷键，可一键复制。
- **API 设置**：菜单保存成功时会触发 loading bar 渐变反馈；若必填项缺失则以红色提示条提醒，未勾选 “Remember” 时不会将配置写入本地。

## 3. 数据持久化与安全

- **IndexedDB**：仅使用 `SubformWorkspace/canvas` 单表存储完整快照（节点、连线、画布 transform、内容、图片 DataURL）。每次结构性变更后调用 `persistState()` 写回 `state` 键。
- **Undo/Redo**：维护 100 步以内的快照栈，`pushUndo(description, snapshot)` 在操作前记录旧状态，`undo()/redo()` 通过 `restoreSnapshot()` 还原。
- **LocalStorage**：持久化 API 配置（baseUrl、apiKey、model、remember）。若用户未开启 “Remember”，保存后会清除本地副本。
- **隐私策略**：所有数据停留在浏览器；模型调用完全由用户配置的接口承担，无额外埋点或远端同步。

## 4. 技术实现要点

### 4.1 前端栈与渲染
- **技术栈**：纯 HTML + CSS + 原生 JavaScript，未使用 React/Vue 等框架。
- **渲染结构**：
  - 背景网格：`<canvas>` + CSS 渐变。
  - 节点：普通 DOM 结构（div + textarea）。
  - 连线：`<svg>` path + 线性渐变实现高亮/脉冲动画。
- **动画驱动**：`requestAnimationFrame` 循环更新弹性连线、虚线偏移、滚动条显隐等动态效果。

### 4.2 业务逻辑
- **状态管理**：利用单一 `state` 对象维护 notes、connections、selection、undoStack 等；通过事件监听直接操作 DOM，无额外类封装。
- **生成管线**：
  - `buildContext()` 逐级回溯连接形成提示文本。
  - `triggerGeneration()` 在本地创建输出节点、连线并调用 `callModel()`。
  - `callModel()` 根据当前设置构造 `fetch` 请求（JSON body + `Authorization`），并处理非 2xx 响应。
- **图片处理**：剪贴板/拖拽图片后使用 `FileReader` 转为 DataURL，创建 `type: 'image'` 节点并随快照持久化。

### 4.3 性能策略
- **弹性连线**：对每条连线维护“弹簧”状态，拖动时动态更新控制点，保持连线顺滑。
- **网格对齐**：节点移动、缩放、复制均遵循 10px 网格，减少重绘抖动。
- **节流同步**：通过定时器触发 `syncNotePositions()`，避免频繁写入数据库；滚动条和动画均带有淡入淡出节奏，减轻渲染压力。

## 5. 埋点与监控

复刻版本未接入第三方埋点、性能监控或错误上报；如需分析，可在关键函数中自行插入埋点逻辑或引入轻量 SDK。

## 6. 能力边界与缺失功能

- 当前未提供富文本/Markdown、AI 自动推荐连线、多人协作、云同步、版本控制、右键菜单等功能。
- 移动端进入页面会渲染只读欢迎卡并禁用交互，提示使用桌面端。
- API 配置仅存在本地（可选择不持久化），当前仅支持一次性 JSON 响应，如需流式或其他协议需扩展 `callModel()`。

## 7. 复刻实现建议

1. **优先还原画布交互**：无限画布、快捷键、弹性连线是核心体验，需要优先实现。
2. **复制数据结构**：借鉴 IndexedDB + Undo/Redo 方案，确保刷新后状态恢复与操作可逆。
3. **抽象模型调用层**：将 `anthropic` 接口封装，便于替换为 Volcengine DeepSeek 等自定义模型。
4. **保持本地隐私**：沿用本地存储策略，或在引入云同步前明确隐私提示。

通过以上分析，可以清晰掌握 subform.ai 的真实能力与实现方式，为后续复刻或二次开发提供准确的设计参考。
