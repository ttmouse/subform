# Subform 交互用例排查清单

记录对照原始 Subform.ai 站点的交互用例测试结果，状态使用：

- ✅ 已完成（行为与目标站一致或可接受）
- ⚠️ 部分完成（核心功能可用但细节存在差异）
- ❌ 未完成（缺失或与目标行为差异明显）

## 画布与布局

| 用例 | 状态 | 备注 |
| --- | --- | --- |
| 背景网格暗色+双层粒子效果 | ✅ | 背景色、粒子层次、光晕与缩放自适配对标参考站 |
| 无限画布与初始居中 | ✅ | 画布尺寸 50000×50000，`centerCanvas()` 居中视图 |
| 空格拖拽/中键平移 | ✅ | `startPan` 支持空格按下拖拽及中键拖拽 |
| 鼠标滚轮平移 | ✅ | 普通滚轮事件按 delta 平滑移动画布 |
| Ctrl/Cmd + 滚轮缩放 | ✅ | 以光标为中心缩放，范围 0.125–4 |
| Cmd/Ctrl + 0 重置视图 | ✅ | 调用 `centerCanvas()` |
| Cmd/Ctrl + / − 分级缩放 | ❌ | 尚未实现分级缩放快捷键 |
| 自定义滚动条显隐与拖动 | ✅ | 依据指针活动淡入，拖动可调整视图位置 |

## 节点创建与编辑

| 用例 | 状态 | 备注 |
| --- | --- | --- |
| 双击画布创建输入节点 | ✅ | 新节点自动选中并聚焦 |
| 双击连接点创建连线节点 | ❌ | 暂未支持；仅手动拖拽连线 |
| Alt/Option 拖拽复制节点 | ⚠️ | 复制节点本体，但不复制既有连线 |
| 节点八向拖拽缩放 | ✅ | 尺寸遵循 10px 网格对齐 |
| Shift 多选节点 | ✅ | 支持 Shift 逐个添加/移除选择 |
| 框选多选 | ❌ | `selectionOverlay` 未启用，缺失框选矩形 |
| 方向键移动节点 | ❌ | 未监听方向键移动及 Shift 加速 |
| 粘贴图片生成图片节点 | ✅ | 剪贴板图片可创建 `image` 节点 |
| 拖拽图片到画布创建节点 | ❌ | 拖拽图片未处理 |
| 输出节点 Copy 按钮 | ✅ | 写入系统剪贴板 |

## 连线与生成

| 用例 | 状态 | 备注 |
| --- | --- | --- |
| 连接点拖拽创建连线 | ✅ | 贝塞尔路径生成，生成时自动连线 |
| 连线弹性/发光动画 | ❌ | 当前为静态路径，缺少动画效果 |
| 连线高亮选中与删除反馈 | ✅ | 支持连接线点击选中、Shift 多选与 Delete 删除，选中状态高亮 |
| Esc 取消连线或选中连线 | ❌ | Esc 仅用于关闭配置面板 |
| Enter 触发生成 | ✅ | 输入节点回车触发 `triggerGeneration` |
| 上下文递归拼接 | ✅ | `buildContext()` 回溯上游文本及图片占位 |

## 快捷键

| 用例 | 状态 | 备注 |
| --- | --- | --- |
| Cmd/Ctrl + Z 撤销 | ✅ | `undo()` 工作正常 |
| Shift + Cmd/Ctrl + Z 重做 | ✅ | `redo()` 工作正常 |
| Delete/Backspace 删除选中项 | ✅ | 删除节点同时清理连线 |

## 持久化与设置

| 用例 | 状态 | 备注 |
| --- | --- | --- |
| IndexedDB 保存画布快照 | ✅ | 节点、连线、transform 均持久化到 `canvas/state` |
| LocalStorage 记忆 API 设置 | ✅ | Remember 选中时保存，未选中清除 |
| Undo 栈限制 100 步 | ✅ | 超出后丢弃最早记录 |

## Onboarding 与移动端

| 用例 | 状态 | 备注 |
| --- | --- | --- |
| 默认欢迎卡与快捷键卡 | ✅ | 首次加载自动创建 |
| 移动端只读提示 | ✅ | 768px 以下显示阻断提示，隐藏画布 |

## 额外功能（非原站）

| 用例 | 状态 | 备注 |
| --- | --- | --- |
| 消息抓取配置面板 | ✅ | 新增 Webhook 配置，存储于 `subform-capture-config` |
