# Triage Labels

## Issue 状态机

| Label | 含义 |
|-------|------|
| `needs-triage` | 还没有判断是否可执行 |
| `needs-info` | 信息不足，不能直接做 |
| `ready-for-agent` | 可以交给 Agent 执行 |
| `ready-for-human` | 需要人判断或验收 |
| `blocked` | 被外部条件阻塞 |
| `wontfix` | 明确不做 |

## 来源

- generated_by_hermes
